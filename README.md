# Multi-Agent AI — MVP

A modular, stateful, and gamified AI agent system.  
Agents: Planner, Researcher, and Reviewer.

## Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
